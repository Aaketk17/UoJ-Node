doctype html
html
  head
    title #{title}
    link(rel='stylesheet', href='/css/style.css')
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body
    main
        header.header
            h1 #{title}
        header.header
            h2 #{name}
        .content
            h2 #{time}